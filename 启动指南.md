# Mizuki 博客本地预览指南

## 📋 前置要求

确保你已经安装：
- Node.js (v18 或更高版本)
- pnpm (推荐) 或 npm

## 🚀 启动步骤

### 1. 进入 mizuki 目录

```bash
cd mizuki
```

### 2. 安装依赖（首次运行）

```bash
# 使用 pnpm（推荐）
pnpm install

# 或使用 npm
npm install
```

### 3. 启动开发服务器

```bash
# 使用 pnpm
pnpm dev

# 或使用 npm
npm run dev
```

### 4. 访问本地网站

启动成功后，在浏览器中访问：
- **本地地址**: http://localhost:4321
- **网络地址**: http://192.168.x.x:4321 (局域网访问)

## 📝 常用命令

### 开发模式
```bash
npm run dev          # 启动开发服务器
npm run start        # 同上
```

### 构建生产版本
```bash
npm run build        # 构建静态网站到 dist 目录
npm run preview      # 预览构建后的网站
```

### 内容管理
```bash
npm run new-post     # 创建新文章
npm run sync-content # 同步内容
```

### 代码检查
```bash
npm run check        # 检查 Astro 代码
npm run format       # 格式化代码
npm run lint         # 代码检查
```

## 🎨 新增功能使用

### 1. 启用交互式背景

在 `src/layouts/Layout.astro` 中添加：

```astro
---
import InteractiveBackground from '@/components/InteractiveBackground.svelte';
---

<InteractiveBackground client:load />
```

### 2. 启用滚动进度条

```astro
---
import ScrollProgress from '@/components/ScrollProgress.svelte';
---

<ScrollProgress client:load />
```

### 3. 启用主题定制器

```astro
---
import ThemeCustomizer from '@/components/ThemeCustomizer.svelte';
---

<ThemeCustomizer client:load />
```

### 4. 使用增强版文章卡片

在文章列表页面中：

```astro
---
import EnhancedPostCard from '@/components/EnhancedPostCard.astro';
---

<EnhancedPostCard entry={post} enhanced={true} />
```

### 5. 添加高级搜索

```astro
---
import AdvancedSearch from '@/components/AdvancedSearch.svelte';
---

<AdvancedSearch client:load />
```

### 6. 集成评论系统

在文章页面中：

```astro
---
import CommentSystem from '@/components/CommentSystem.svelte';
---

<CommentSystem 
  postId={entry.id} 
  postTitle={entry.data.title} 
  client:load 
/>
```

## ⚙️ 配置说明

在 `src/config.ts` 中已添加新的配置选项：

```typescript
enhancements: {
  interactiveBackground: true,  // 交互式背景
  scrollProgress: true,         // 滚动进度
  enhancedAnimations: true,     // 增强动画
  glassEffect: true,            // 玻璃拟态效果
  themeCustomizer: true,        // 主题定制器
  advancedSearch: true,         // 高级搜索
  commentSystem: true,          // 评论系统
}
```

## 🎨 样式文件说明

新增的样式文件已自动导入到 `src/styles/main.css`：

- `enhanced-animations.css` - 增强动画效果
- `glass-morphism.css` - 玻璃拟态效果
- `background-patterns.css` - 背景图案

## 🐛 常见问题

### 问题 1: 端口被占用

如果 4321 端口被占用，可以修改端口：

```bash
npm run dev -- --port 3000
```

### 问题 2: 依赖安装失败

尝试清除缓存后重新安装：

```bash
rm -rf node_modules package-lock.json
npm install
```

### 问题 3: 构建错误

检查 Node.js 版本：

```bash
node --version  # 应该 >= 18.0.0
```

## 📦 部署到 GitHub Pages

### 1. 构建网站

```bash
npm run build
```

### 2. 部署

```bash
# 如果使用 GitHub Actions，推送代码即可自动部署
git add .
git commit -m "Update blog"
git push
```

## 🔗 相关链接

- [Astro 文档](https://docs.astro.build/)
- [Mizuki 主题文档](https://github.com/saicaca/fuwari)
- [你的博客地址](https://dandingbobaishi.github.io/)

## 💡 提示

1. 开发时修改文件会自动热重载
2. 新增文章后需要重启开发服务器
3. 图片建议放在 `public/assets/` 目录下
4. 使用 WebP 格式可以提升加载速度

## 🎯 下一步

1. 启动开发服务器查看效果
2. 根据需要调整配置
3. 创建新文章测试功能
4. 自定义主题颜色和样式
5. 部署到生产环境

---

**注意**: 你的项目中有两个博客系统：
- `mizuki/` - Astro 博客（推荐使用）
- `myblogs/` - Hexo 博客（旧版本）

建议专注使用 Mizuki (Astro) 版本，它更现代且功能更强大。
