# 🚀 Mizuki 博客快速开始

## 📌 三步部署到 GitHub Pages

### 第一步：配置 SSH 密钥（仅需一次）

#### 1. 生成密钥对

在项目根目录打开终端，运行：

```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com" -f mizuki-deploy-key -N ""
```

会生成两个文件：
- `mizuki-deploy-key` - 私钥
- `mizuki-deploy-key.pub` - 公钥

#### 2. 配置公钥到 Pages 仓库

1. 打开 https://github.com/dandingbobaishi/dandingbobaishi.github.io/settings/keys
2. 点击 **Add deploy key**
3. 填写：
   - Title: `MIZUKI_DEPLOY_KEY`
   - Key: 复制 `mizuki-deploy-key.pub` 的全部内容
   - ✅ 勾选 **Allow write access**
4. 点击 **Add key**

#### 3. 配置私钥到源码仓库

1. 打开你的源码仓库的 Settings
2. 进入 **Secrets and variables** → **Actions**
3. 点击 **New repository secret**
4. 填写：
   - Name: `DEPLOY_KEY`
   - Secret: 复制 `mizuki-deploy-key` 的全部内容（私钥）
5. 点击 **Add secret**

#### 4. 删除本地密钥文件（重要！）

```bash
del mizuki-deploy-key
del mizuki-deploy-key.pub
```

---

### 第二步：推送代码

#### 方法 A：使用一键部署脚本（推荐）

双击运行 `deploy.bat`，脚本会自动：
- ✅ 检查环境
- ✅ 安装依赖
- ✅ 构建项目
- ✅ 提交代码
- ✅ 推送到 GitHub

#### 方法 B：手动推送

```bash
cd mizuki
git add .
git commit -m "Initial commit"
git push origin master
```

---

### 第三步：等待自动部署

1. 推送后，访问 https://github.com/你的用户名/你的仓库/actions
2. 查看 "Auto Deploy Mizuki Blog to GitHub Pages" 工作流
3. 等待构建完成（约 2-5 分钟）
4. 访问 https://dandingbobaishi.github.io/ 查看你的博客！

---

## 📝 日常使用

### 创建新文章

1. 在 `src/content/posts/` 目录下创建新的 `.md` 文件
2. 添加 front-matter：

```yaml
---
title: 我的第一篇文章
published: 2024-01-15
description: 这是文章描述
tags: [标签1, 标签2]
category: 分类
---

这里是文章内容...
```

### 本地预览

```bash
cd mizuki
npm run dev
```

访问 http://localhost:4321

### 发布更新

```bash
# 方法 1: 使用脚本（推荐）
双击 deploy.bat

# 方法 2: 手动推送
git add .
git commit -m "更新内容"
git push origin master
```

---

## 🎨 自定义配置

### 修改网站信息

编辑 `src/config.ts`：

```typescript
export const siteConfig = {
  title: "你的博客名称",
  subtitle: "副标题",
  siteURL: "https://dandingbobaishi.github.io/",
  // ... 更多配置
};
```

### 修改主题颜色

```typescript
themeColor: {
  hue: 230,  // 0-360，调整主题色相
  fixed: false,
},
```

### 修改导航栏

```typescript
export const navBarConfig = {
  links: [
    LinkPreset.Home,
    LinkPreset.Archive,
    // 添加自定义链接
    {
      name: "关于",
      url: "/about/",
      icon: "material-symbols:info",
    },
  ],
};
```

---

## 🔧 常用命令

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 创建新文章
npm run new-post

# 代码格式化
npm run format

# 代码检查
npm run check
```

---

## 📚 文件结构

```
mizuki/
├── src/
│   ├── content/
│   │   └── posts/          # 📝 文章目录
│   ├── components/         # 🧩 组件
│   ├── styles/            # 🎨 样式
│   ├── layouts/           # 📐 布局
│   └── config.ts          # ⚙️ 配置文件
├── public/                # 📦 静态资源
│   └── assets/           # 图片等资源
├── deploy.bat            # 🚀 部署脚本
└── package.json          # 📋 项目配置
```

---

## 🎯 新功能使用

### 1. 启用交互式背景

编辑 `src/layouts/Layout.astro`，添加：

```astro
---
import InteractiveBackground from '@/components/InteractiveBackground.svelte';
---

<InteractiveBackground client:load />
```

### 2. 启用滚动进度条

```astro
---
import ScrollProgress from '@/components/ScrollProgress.svelte';
---

<ScrollProgress client:load />
```

### 3. 启用主题定制器

```astro
---
import ThemeCustomizer from '@/components/ThemeCustomizer.svelte';
---

<ThemeCustomizer client:load />
```

### 4. 使用增强搜索

```astro
---
import AdvancedSearch from '@/components/AdvancedSearch.svelte';
---

<AdvancedSearch client:load />
```

### 5. 添加评论系统

```astro
---
import CommentSystem from '@/components/CommentSystem.svelte';
---

<CommentSystem postId={entry.id} postTitle={entry.data.title} client:load />
```

---

## ❓ 常见问题

### Q: 推送后网站没更新？

1. 检查 GitHub Actions 是否成功运行
2. 清除浏览器缓存（Ctrl+Shift+R）
3. 等待 5-10 分钟

### Q: 构建失败？

1. 查看 Actions 日志中的错误信息
2. 本地运行 `npm run build` 测试
3. 检查文章的 front-matter 格式是否正确

### Q: 图片不显示？

1. 图片放在 `public/assets/` 目录
2. 使用相对路径：`/assets/image.jpg`
3. 或使用完整 URL

### Q: 如何添加自定义域名？

1. 在 Pages 仓库根目录添加 `CNAME` 文件
2. 内容为你的域名：`blog.example.com`
3. 在域名提供商处添加 CNAME 记录

---

## 🔗 相关链接

- **你的博客**: https://dandingbobaishi.github.io/
- **GitHub 仓库**: https://github.com/dandingbobaishi
- **Astro 文档**: https://docs.astro.build/
- **部署详细指南**: [部署指南.md](./部署指南.md)
- **启动指南**: [启动指南.md](./启动指南.md)

---

## 💡 提示

1. **定期备份**: 源码仓库就是你的备份
2. **本地测试**: 推送前先本地预览
3. **查看日志**: 部署失败时查看 Actions 日志
4. **保持更新**: 定期更新依赖包

---

## 🎉 开始创作

现在你已经准备好了！开始创作你的第一篇文章吧：

1. 在 `src/content/posts/` 创建 `my-first-post.md`
2. 编写内容
3. 运行 `deploy.bat` 部署
4. 访问你的博客查看效果

祝你写作愉快！✨
